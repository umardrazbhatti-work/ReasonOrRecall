# runs: logs, adapters, results — keep the folder, ignore contents
runs/*
!runs/.gitkeep
runs_*/

# Kaggle run outputs downloaded for review — local only
Results/*
!Results/README.md

# data caches / the built clean set / the Kaggle data zip
data/
dist/
*.arrow
*.parquet

# model artifacts
*.safetensors
*.bin
*.ckpt
checkpoints/

# secrets
.env

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
