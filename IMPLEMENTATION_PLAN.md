# Implementation plan -> see docs/ROADMAP.md

Since 2026-09-26 the plan and tracker is **`docs/ROADMAP.md`** (approved v1.0):
phases P0-P10, task IDs, gates, compute budget, run log and decision log.
Check progress with `python scripts/roadmap.py`.

The previous checklist is in git history (commit dea4b82 and earlier).
