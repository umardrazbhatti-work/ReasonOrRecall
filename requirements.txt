# Compatible ranges — resolve exact versions in the Kaggle image and commit a
# lockfile (pip freeze > requirements.lock). Framework/tests need only pyyaml +
# pytest; the rest are for the ML modules you implement.

# framework + tests
pyyaml>=6.0
pytest>=7.4

# ML stack (Jobs 1-3). Torch/bitsandbytes builds are environment-specific.
torch>=2.1
transformers>=4.44
peft>=0.12
trl>=0.9
bitsandbytes>=0.43
accelerate>=0.33
datasets>=2.20
sentencepiece>=0.2
tqdm>=4.66

# optional: faster inference / teacher serving (Job 3/4)
# vllm>=0.5
# optional: teacher via API (Job 4)
# openai>=1.40        # or the client for your chosen open-70B provider
