# Compatible ranges (minimums = the tested versions). Exact tested pins are in
# requirements.lock. Framework/tests need only pyyaml +
# pytest; the rest are for the ML modules you implement.

# framework + tests
pyyaml>=6.0
pytest>=7.4
numpy>=2.0            # ror.stats (bootstrap CIs)

# ML stack (Jobs 1-3). Torch/bitsandbytes builds are environment-specific.
torch>=2.4
transformers>=5.0
peft>=0.19
trl>=1.14
bitsandbytes>=0.50
accelerate>=1.13
datasets>=5.0
sentencepiece>=0.2
tqdm>=4.66

# run reports (figures): scripts/make_report.py, ror.report
matplotlib>=3.10

# optional: faster inference / teacher serving (Job 3/4)
# vllm>=0.5
# optional: teacher via API (Job 4)
# openai>=1.40        # or the client for your chosen open-70B provider
